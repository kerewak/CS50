new language called SQL

C ve python'a kıyasla küçük bir dildir.
Az iş yapar ama o işi güçlü yapar.
Bu, veritabanlarını sorgulamak ve bunlara veri depolamak için kullanılan bir dildir.

https://httpducks.com/404


flat-file database

Bir çok veritabanı var ama en basiti .csv file.

Python aslında csv dosyaları için native support'a sahip.


import csv

with open("favorites.csv", "r") as file:
  reader = csv.DictReader(file)
  for row in reader:
    favorite = row["language"]
    print(favorite)



import csv

with open("favorites.csv", "r") as file:
  reader = csv.DictReader(file)
  counts = {}
  for row in reader:
    favorite = row["language"]
    if favorite in counts:
      counts[favorite] += 1
    else:
      counts[favorite] = 1

//def get_value(language):
//return counts[language]

for favorite in sorted(counts, key=lambda problem: counts[problem], reverse=True):
  print(f"{favorite}: {counts[favorite]}")


Sadece bir kere kullanmak için bir fonksiyon oluşturmakla uğraşmak istemeyiz.
Anonim bir fonksiyon, ismi yok, "lambda"


lambda language: counts[language]

lambda argüman: return





relational database

MySQL, PostgreSQL, Microsoft SQL, Server, Oracle, MariaDB


SQL sadece dört temel şey yapar: CRUD

CREATE
READ
UPDATE
DELETE

CREATE, INSERT
SELECT
UPDATE
DELETE, DROP
...


CREATE TABLE table (column type, ...);



sqlite3

.mode csv
.import FILE TABLE

SELECT columns FROM table;


SQL fonksiyonları

AVG
COUNT
DISTINCT
LOWER
MAX
MIN
UPPER
...




















